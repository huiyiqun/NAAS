
/*
  Static helpers index file
 */

// module dependencies

module.exports = {
  // install static helpers on app.locals
  // @param {express} app app to install helper
  installStaticHelper: (app) => {
    // install constants static helpers:
    //app.locals.c = constants
  }
}
